# =============================================================================
# SAR Module Template - Environment Variables
# =============================================================================
#
# Copy this file to .env.local and fill in your values:
#   cp .env.template .env.local
#
# IMPORTANT SECURITY NOTES:
# - NEVER commit .env.local to version control
# - NEVER expose server-side secrets (like KEYCLOAK_CLIENT_SECRET) to the browser
# - Only variables prefixed with NEXT_PUBLIC_ are accessible in the browser
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration (REQUIRED)
# -----------------------------------------------------------------------------

# Backend API base URL
# Development: http://localhost:8080
# Production: https://api.centri.id
NEXT_PUBLIC_API_URL=https://api.centri.id

# -----------------------------------------------------------------------------
# Application URLs (REQUIRED)
# -----------------------------------------------------------------------------

# This module's base URL
# Development: http://localhost:3900
# Production: https://hc.centri.id (or your module's domain)
NEXT_PUBLIC_APP_URL=https://hc.centri.id

# Main BANTAL application URL (for SSO redirects)
# Development: http://localhost:3000
# Production: https://web.centri.id
NEXT_PUBLIC_MAIN_APP_URL=https://web.centri.id

# -----------------------------------------------------------------------------
# Module Configuration (OPTIONAL)
# -----------------------------------------------------------------------------

# Internal module name (lowercase, hyphenated)
# Used for logging, debugging, and internal references
# Default: sar-module
NEXT_PUBLIC_MODULE_NAME=human-capital

# Display name shown in UI (page titles, headers, etc.)
# Default: SAR Module
NEXT_PUBLIC_MODULE_DISPLAY_NAME=Human Capital

# Module version (semver format)
# Default: 0.1.0
NEXT_PUBLIC_MODULE_VERSION=0.1.0

# -----------------------------------------------------------------------------
# Keycloak Configuration (OPTIONAL - only if using Keycloak directly)
# -----------------------------------------------------------------------------
# Note: Most modules don't need these as authentication is handled by the
# main BANTAL application. Only set these if your module needs direct
# Keycloak integration.

# Keycloak server base URL
# Example: https://auth.centri.id
# KEYCLOAK_BASE_URL=

# Keycloak realm name
# Example: BANTAL
# KEYCLOAK_REALM=

# Keycloak client ID
# Example: bantal-web
# KEYCLOAK_CLIENT_ID=

# Keycloak redirect URI (where Keycloak sends users after login)
# Example: https://api.centri.id/auth/callback
# KEYCLOAK_REDIRECT_URI=

# Keycloak client secret (SERVER-SIDE ONLY - NEVER expose to browser!)
# ⚠️ CRITICAL: This should ONLY be used in API routes or server components
# ⚠️ NEVER prefix with NEXT_PUBLIC_ or it will be exposed to the browser
# ⚠️ Store this securely and never commit to version control
# KEYCLOAK_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Node Environment (OPTIONAL)
# -----------------------------------------------------------------------------

# Application environment
# Values: development | production | test
# Default: development
NODE_ENV=development

# -----------------------------------------------------------------------------
# Additional Notes
# -----------------------------------------------------------------------------
#
# Cookie-based Authentication:
# This template uses cookie-based SSO authentication. The following cookies
# are automatically managed by the backend API:
#   - auth_session (15 min) - JWT access token
#   - refresh_token (24 hours) - Refresh token
#
# These cookies are set on the .centri.id domain for cross-subdomain sharing.
#
# Environment Variable Validation:
# All environment variables are validated at runtime using Zod schemas.
# See lib/config/env.ts for the validation logic.
# If validation fails, you'll see detailed error messages in the console.
#
# For Development:
# - Use localhost URLs for all services
# - Ensure your backend API is running
# - Cookies may not work across different localhost ports
#
# For Production:
# - Use HTTPS for all URLs
# - Ensure all services are on the same domain (.centri.id)
# - Verify CORS settings on the backend API
#
# =============================================================================
